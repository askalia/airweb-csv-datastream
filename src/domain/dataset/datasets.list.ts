import { IDatasetRecord } from './models/dataset.abstract.model';

const datasets: IDatasetRecord[] = [
  {
    id: 'order789',
    target: 'order',
    filters: [],
  },
];

export default datasets;
