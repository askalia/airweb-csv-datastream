import { FormatSupportedPipe } from '../format-supported.pipe';

describe('FormatSupportedPipe', () => {
  it('should be defined', () => {
    expect(new FormatSupportedPipe()).toBeDefined();
  });
});
